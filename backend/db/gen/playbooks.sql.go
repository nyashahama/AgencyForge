// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: playbooks.sql

package dbgen

import (
	"context"
	"time"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const countPlaybooksByAgency = `-- name: CountPlaybooksByAgency :one
SELECT COUNT(*)
FROM playbooks
WHERE agency_id = $1
  AND status <> 'archived'
`

func (q *Queries) CountPlaybooksByAgency(ctx context.Context, agencyID uuid.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countPlaybooksByAgency, agencyID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createPlaybook = `-- name: CreatePlaybook :one
INSERT INTO playbooks (
  agency_id,
  name,
  category,
  owner_user_id,
  status,
  body,
  published_at
) VALUES (
  $1, $2, $3, $4, $5, $6, $7
)
RETURNING id, agency_id, name, category, owner_user_id, status, body, published_at, created_at, updated_at
`

type CreatePlaybookParams struct {
	AgencyID    uuid.UUID          `json:"agency_id"`
	Name        string             `json:"name"`
	Category    string             `json:"category"`
	OwnerUserID pgtype.UUID        `json:"owner_user_id"`
	Status      string             `json:"status"`
	Body        string             `json:"body"`
	PublishedAt pgtype.Timestamptz `json:"published_at"`
}

func (q *Queries) CreatePlaybook(ctx context.Context, arg CreatePlaybookParams) (Playbook, error) {
	row := q.db.QueryRow(ctx, createPlaybook,
		arg.AgencyID,
		arg.Name,
		arg.Category,
		arg.OwnerUserID,
		arg.Status,
		arg.Body,
		arg.PublishedAt,
	)
	var i Playbook
	err := row.Scan(
		&i.ID,
		&i.AgencyID,
		&i.Name,
		&i.Category,
		&i.OwnerUserID,
		&i.Status,
		&i.Body,
		&i.PublishedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getPlaybookByIDAndAgency = `-- name: GetPlaybookByIDAndAgency :one
SELECT id, agency_id, name, category, owner_user_id, status, body, published_at, created_at, updated_at
FROM playbooks
WHERE agency_id = $1
  AND id = $2
  AND status <> 'archived'
LIMIT 1
`

type GetPlaybookByIDAndAgencyParams struct {
	AgencyID uuid.UUID `json:"agency_id"`
	ID       uuid.UUID `json:"id"`
}

func (q *Queries) GetPlaybookByIDAndAgency(ctx context.Context, arg GetPlaybookByIDAndAgencyParams) (Playbook, error) {
	row := q.db.QueryRow(ctx, getPlaybookByIDAndAgency, arg.AgencyID, arg.ID)
	var i Playbook
	err := row.Scan(
		&i.ID,
		&i.AgencyID,
		&i.Name,
		&i.Category,
		&i.OwnerUserID,
		&i.Status,
		&i.Body,
		&i.PublishedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getPlaybookSummaryByIDAndAgency = `-- name: GetPlaybookSummaryByIDAndAgency :one
SELECT
  p.id, p.agency_id, p.name, p.category, p.owner_user_id, p.status, p.body, p.published_at, p.created_at, p.updated_at,
  COALESCE(u.name, u.email, '') AS owner_name
FROM playbooks p
LEFT JOIN users u
  ON u.id = p.owner_user_id
WHERE p.agency_id = $1
  AND p.id = $2
  AND p.status <> 'archived'
LIMIT 1
`

type GetPlaybookSummaryByIDAndAgencyParams struct {
	AgencyID uuid.UUID `json:"agency_id"`
	ID       uuid.UUID `json:"id"`
}

type GetPlaybookSummaryByIDAndAgencyRow struct {
	ID          uuid.UUID          `json:"id"`
	AgencyID    uuid.UUID          `json:"agency_id"`
	Name        string             `json:"name"`
	Category    string             `json:"category"`
	OwnerUserID pgtype.UUID        `json:"owner_user_id"`
	Status      string             `json:"status"`
	Body        string             `json:"body"`
	PublishedAt pgtype.Timestamptz `json:"published_at"`
	CreatedAt   time.Time          `json:"created_at"`
	UpdatedAt   time.Time          `json:"updated_at"`
	OwnerName   string             `json:"owner_name"`
}

func (q *Queries) GetPlaybookSummaryByIDAndAgency(ctx context.Context, arg GetPlaybookSummaryByIDAndAgencyParams) (GetPlaybookSummaryByIDAndAgencyRow, error) {
	row := q.db.QueryRow(ctx, getPlaybookSummaryByIDAndAgency, arg.AgencyID, arg.ID)
	var i GetPlaybookSummaryByIDAndAgencyRow
	err := row.Scan(
		&i.ID,
		&i.AgencyID,
		&i.Name,
		&i.Category,
		&i.OwnerUserID,
		&i.Status,
		&i.Body,
		&i.PublishedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.OwnerName,
	)
	return i, err
}

const listPlaybooksByAgency = `-- name: ListPlaybooksByAgency :many
SELECT
  p.id, p.agency_id, p.name, p.category, p.owner_user_id, p.status, p.body, p.published_at, p.created_at, p.updated_at,
  COALESCE(u.name, u.email, '') AS owner_name
FROM playbooks p
LEFT JOIN users u
  ON u.id = p.owner_user_id
WHERE p.agency_id = $1
  AND p.status <> 'archived'
ORDER BY p.updated_at DESC, p.created_at DESC
LIMIT $2 OFFSET $3
`

type ListPlaybooksByAgencyParams struct {
	AgencyID uuid.UUID `json:"agency_id"`
	Limit    int32     `json:"limit"`
	Offset   int32     `json:"offset"`
}

type ListPlaybooksByAgencyRow struct {
	ID          uuid.UUID          `json:"id"`
	AgencyID    uuid.UUID          `json:"agency_id"`
	Name        string             `json:"name"`
	Category    string             `json:"category"`
	OwnerUserID pgtype.UUID        `json:"owner_user_id"`
	Status      string             `json:"status"`
	Body        string             `json:"body"`
	PublishedAt pgtype.Timestamptz `json:"published_at"`
	CreatedAt   time.Time          `json:"created_at"`
	UpdatedAt   time.Time          `json:"updated_at"`
	OwnerName   string             `json:"owner_name"`
}

func (q *Queries) ListPlaybooksByAgency(ctx context.Context, arg ListPlaybooksByAgencyParams) ([]ListPlaybooksByAgencyRow, error) {
	rows, err := q.db.Query(ctx, listPlaybooksByAgency, arg.AgencyID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListPlaybooksByAgencyRow{}
	for rows.Next() {
		var i ListPlaybooksByAgencyRow
		if err := rows.Scan(
			&i.ID,
			&i.AgencyID,
			&i.Name,
			&i.Category,
			&i.OwnerUserID,
			&i.Status,
			&i.Body,
			&i.PublishedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.OwnerName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updatePlaybook = `-- name: UpdatePlaybook :one
UPDATE playbooks
SET
  name = $3,
  category = $4,
  status = $5,
  body = $6,
  published_at = $7,
  updated_at = NOW()
WHERE agency_id = $1
  AND id = $2
  AND status <> 'archived'
RETURNING id, agency_id, name, category, owner_user_id, status, body, published_at, created_at, updated_at
`

type UpdatePlaybookParams struct {
	AgencyID    uuid.UUID          `json:"agency_id"`
	ID          uuid.UUID          `json:"id"`
	Name        string             `json:"name"`
	Category    string             `json:"category"`
	Status      string             `json:"status"`
	Body        string             `json:"body"`
	PublishedAt pgtype.Timestamptz `json:"published_at"`
}

func (q *Queries) UpdatePlaybook(ctx context.Context, arg UpdatePlaybookParams) (Playbook, error) {
	row := q.db.QueryRow(ctx, updatePlaybook,
		arg.AgencyID,
		arg.ID,
		arg.Name,
		arg.Category,
		arg.Status,
		arg.Body,
		arg.PublishedAt,
	)
	var i Playbook
	err := row.Scan(
		&i.ID,
		&i.AgencyID,
		&i.Name,
		&i.Category,
		&i.OwnerUserID,
		&i.Status,
		&i.Body,
		&i.PublishedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
